This projects implements the dataplane of a router. Currently capable of handling IPv4, ICMP and the ARP protocol is planned to extend to VLAN, dynamic routing and IPv6


To use the checker provided by the PCOM team, run ./checker/checker.sh
